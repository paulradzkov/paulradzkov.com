---
layout: 'default'
title: 'Блог'
---

<h1>Блог</h1>

<div class="blogroll">
    <% for page in @getCollection("articles").toJSON(): %>
        <section class="post-tizer <%= if page.id is @document.id then 'active' else 'inactive' %>">
            
            <h2><a href="<%= page.url %>"><%= page.title %></a></h2>
			<%- @cuttedContent page.contentRenderedWithoutLayouts %>
			
			<aside class="meta">
				<ul class="meta-data">
					<li class="date"><%= @formatDate(page.created_at,'DD MMMM YYYY') %></li>
					<li class="comments"><a href="<%= page.url %>#disqus_thread" data-disqus-identifier="<%= page.disqusid %>" >Комментарии</a></li>
					<li class="tags-list">
						<% for tag in page.tags : %>
						<a class="tag" href="<%= @getTagUrl(tag) %>"><%= tag %></a>
						<% end %>
					</li>
				</ul>
			</aside>

        </section>
    <% end %>
</div>
